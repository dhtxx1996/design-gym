Total Points: 100

Notes: The key to a full-score answer is demonstrating a complete understanding of the computational protein design workflow for pH-sensitive monomer destabilization for a de novo designed NTF2 fold (PDB ID 5L33, under data/scaffold.pdb), aiming for binding at neutral pH (7) and dissociation at low pH (5.5): (1) correctly identifying buried core residues suitable for pH sensing (SASA < 0.25), (2) attempting to use Pyrosetta HBNet to find buried His-containing hydrogen bond networks (His–His, His–Arg, His–Lys) within the core, and using alternative methods only when Pyrosetta is unavailable, (3) filtering networks based on primary histidine hydrogen bond energy threshold (< -0.5 kcal/mol), (4) using ProteinMPNN to redesign environmental residues around the selected network while fixing the network residues, and (5) validating designs through structure prediction (ESMFold/AlphaFold) and filtering based on confidence metrics (pLDDT) and geometric verification that the network is formed. Answers that correctly execute this workflow using Pyrosetta HBNet should receive full credit. Answers using alternative methods (e.g., geometric distance/angle-based network detection) when Pyrosetta is unavailable should receive partial credit as long as they first attempt to use Pyrosetta and demonstrate understanding of the workflow.

Criteria (8):

  Criterion 1: Core Residue Identification
    Description: Evaluates the correct identification of buried residues in the scaffold structure suitable for pH sensing.
    Max Points: 13
    Levels:
      [A] (11 points): Calculates Solvent Accessible Surface Area (SASA) using a standard tool (BioPython, FreeSASA, PyRosetta) and uses a relative SASA threshold < 0.25 times the maximum value to select buried core positions.

      [B] (6 points): Calculates SASA but uses threshold outside the range of 0.1-0.4 (relative), uses absolute SASA values without normalization to maximum, or includes surface-exposed residues in the core selection.

      [C] (0 points): Does not calculate SASA, identifies surface residues as core positions, or does not address core residue identification.

  Criterion 2: HBNet Method Selection
    Description: Evaluates whether the correct method is used for network discovery - Pyrosetta HBNet is preferred, alternative methods receive partial credit when Pyrosetta is unavailable.
    Max Points: 14
    Levels:
      [A] (14 points): Uses PyRosetta HBNet by setting up PyRosetta pose from scaffold PDB structure, configuring and applying HBNet mover, and extracting discovered networks from HBNet output.

      [B] (7 points): Attempts to use Pyrosetta HBNet first (with try/except or availability check), then uses alternative geometric analysis method (distance and angle calculations) to identify networks. OR uses PyRosetta HBNet but searches all residues instead of restricting to core positions, or extracts network information incorrectly.

      [C] (0 points): Uses alternative method without attempting Pyrosetta or good geometry/distance reasoning, or fails to discover any networks.

  Criterion 3: Network Type Screening
    Description: Evaluates whether the screening targets the correct network types: His-His, His-Arg, and His-Lys pairs/networks.
    Max Points: 13
    Levels:
      [A] (13 points): Configures network search to specifically target His-His, His-Arg, and His-Lys pairs/networks, and successfully identifies networks of these types.

      [B] (7 points): Searches for His-containing networks but includes incorrect network types (e.g., His-anionic interactions which are in the opposite direction), or searches for correct types but fails to restrict to core positions where these networks should be located.

      [C] (0 points): Searches for wrong network types (not Histidine involved), or does not specify network types in the search configuration.

  Criterion 4: Histidine H-Bond Energy Filtering
    Description: Evaluates correct filtering of networks based on primary histidine hydrogen bond energy. PyRosetta HBondSet is the preferred method; alternative methods receive partial credit when Pyrosetta is unavailable.
    Max Points: 11
    Levels:
      [A] (11 points): Uses PyRosetta's HBondSet or Rosetta's H-bond energy calculator to identify and score H-bonds involving Histidine ND1/NE2 atoms, retaining networks only if a primary His H-bond has an energy below –0.5 kcal/mol.

      [B] (5 points): Uses alternative geometric criteria (distances, angles) to estimate H-bond strengths and applies equivalent filtering (e.g., distance < 3.5 Å for strong H-bonds). OR uses PyRosetta HBondSet but applies incorrect threshold (wrong sign like > -0.5, or magnitude like < -5.0), measures H-bonds involving wrong atoms (not His ND1/NE2), or filters non-primary histidine H-bonds.

      [C] (0 points): Uses alternative method without attempting Pyrosetta first, uses completely wrong metric for filtering, or does not filter networks by H-bond energy/geometry.

  Criterion 5: ProteinMPNN Sequence Design
    Description: Evaluates redesign of environmental residues around the network using ProteinMPNN.
    Max Points: 19
    Levels:
      [A] (19 points): Uses ProteinMPNN (or equivalent inverse folding tool) with scaffold structure as input, fixes the identity of network residues (forcing His, Arg, or Lys at network positions), allows surrounding environmental residues to mutate, and generates multiple sequence candidates (at least 5-20 sequences).

      [B] (9 points): Uses ProteinMPNN but generates only a single sequence, or manually mutates only network residues without using inverse folding tool for repacking environmental residues, or uses structure-based design but does not fix network residue identities.

      [C] (0 points): Does not perform sequence design, or designs sequences but allows network residues to mutate away from required identities (His, Arg, Lys).

  Criterion 6: Structure Prediction
    Description: Evaluates validation of designed sequences using structure prediction.
    Max Points: 14
    Levels:
      [A] (16 points): Uses a high-accuracy structure prediction tool (ESMFold, AlphaFold2, Boltz-2, Chai-1, or equivalent) to predict structures for multiple design candidates (at least 3-5 sequences) selected based on top inverse folding scores.

      [B] (8 points): Uses high-accuracy prediction tool (ESMFold, AlphaFold2, Boltz-2, Chai-1, or equivalent) but predicts only a single structure without selecting top-scoring variants, or uses low-accuracy prediction method, or predicts structures but does not select candidates based on inverse folding scores.

      [C] (0 points): Does not perform structure prediction, uses completely wrong prediction method, or does not address structural validation.

  Criterion 7: Confidence-Based Filtering
    Description: Evaluates filtering of predicted structures based on confidence metrics.
    Max Points: 6
    Levels:
      [A] (6 points): Calculates mean pLDDT scores for each predicted structure and applies appropriate confidence threshold (mean pLDDT > 70 or > 80 for AlphaFold/ESMFold, or pLDDT > 0.8 for Boltz-2) to select only high-confidence predictions.

      [B] (3 points): Calculates confidence metrics but uses unreasonable threshold (e.g., pLDDT > 0.2 which is too loose, or pLDDT < 0.8 for Boltz-2 which is wrong direction), or applies AlphaFold/ESMFold threshold to Boltz-2 which uses 0-1 scale.

      [C] (0 points): Does not calculate confidence metrics (pLDDT), does not filter predictions, uses completely wrong metric, or does not address confidence-based filtering.

  Criterion 8: Conceptual Understanding - Why Histidine and Why Buried Networks
    Description: Evaluates understanding of key conceptual principles: why histidine is chosen for pH sensing and why networks must be buried.
    Max Points: 10
    Levels:
      [A] (10 points): Explains that histidine is chosen because it has a pKa (~6.0) near physiological/endosomal pH, allowing protonation state changes in the relevant pH range, and explains that networks must be buried (in the core) to experience the pKa shift necessary for sensing physiological pH changes, noting that surface residues would have normal pKa and not shift significantly.

      [B] (5 points): Mentions that histidine has appropriate pKa (~6.0) for pH sensing but does not explain why burial is necessary, OR explains that networks must be buried but does not connect burial to pKa shift mechanism, OR provides only partial explanation of one concept.

      [C] (0 points): Does not explain why histidine is chosen for pH sensing, does not explain why networks must be buried, or provides incorrect explanations.
